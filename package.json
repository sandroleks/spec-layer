{
  "name": "spec-layer-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin that turns components and variables into design-system documentation",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sandroleks/spec-layer.git"
  },
  "bugs": {
    "url": "https://github.com/sandroleks/spec-layer/issues"
  },
  "homepage": "https://github.com/sandroleks/spec-layer#readme",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -p packages/extractor/tsconfig.json --noEmit && tsc -p packages/plugin/tsconfig.json --noEmit && tsc -p packages/proxy/tsconfig.json --noEmit && tsc -p packages/cli/tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "build:plugin": "node packages/plugin/build.mjs",
    "build:cli": "node packages/cli/build.mjs",
    "audit": "npm audit",
    "audit:production": "npm audit --omit=dev",
    "check:nul": "node scripts/check-nul-bytes.mjs",
    "check": "npm run lint && npm run typecheck && npm run check:nul && npm test && npm run build:plugin && npm run build:cli && npm run check:cli-bundle && npm run check:sandbox && npm run check:proxy-dry-run",
    "check:ci": "npm run lint && npm run typecheck && npm run check:nul && npm run test:coverage && npm run build:plugin && npm run build:cli && npm run check:cli-bundle && npm run check:sandbox && npm run check:proxy-dry-run && npm run audit",
    "check:cli-bundle": "node scripts/check-cli-bundle.mjs",
    "check:sandbox": "node scripts/check-main-sandbox.mjs",
    "check:proxy-dry-run": "npm run check:deploy --workspace @spec-layer/proxy"
  },
  "overrides": {
    "brace-expansion": "^5.0.9",
    "minimatch": "^10.2.6",
    "nanoid": "^3.3.18"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.8",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "eslint": "^9.39.4",
    "globals": "^14.0.0",
    "happy-dom": "^20.11.2",
    "js-yaml": "^5.3.0",
    "style-dictionary": "^5.5.2",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.61.0",
    "vitest": "^4.1.8"
  }
}
