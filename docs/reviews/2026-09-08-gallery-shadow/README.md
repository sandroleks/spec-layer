# Original PNG gallery update

Replaced all three gallery files with the supplied 1530 × 990 PNG originals, byte for byte. Preserved their transparency, rounded corners, and shadows. Removed frame rounding, background, clipping, and added image shadows. Updated dimensions and cache versions.

Production build checks passed. Local and live browser verification passed for all three tabs at desktop and mobile sizes, including exact source-file byte equality, native aspect ratio, and image styling. All 59 production HTTP checks passed. Deployment and rollback references are in release.json.
