{
  "name": "@spec-layer/proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "check:deploy": "wrangler deploy --dry-run --config wrangler.toml"
  },
  "dependencies": {
    "@spec-layer/extractor": "*",
    "js-sha256": "^0.11.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260830.1",
    "wrangler": "^4.127.1"
  }
}
